setup:
    addons:
    - plan: heroku-postgresql 
build:
    docker:
        web: Dockerfile 
release:
    image: web 
    command:
        - python manage.py runserver 0.0.0.0:8000 
run:
    web: gunicorn iron_project.wsgi